import styled from "styled-components";

export const Button = styled.button`
  border-radius: 0;
  border: none;
  background-color: unset;
  cursor: pointer;
  margin: 0;

  &:focus-visible {
    outline: none;
  }
`;
